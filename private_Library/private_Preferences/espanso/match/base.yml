# Automatically generated by espanso migration tool
# Original file: default.yml

matches:
  - trigger: ":espanso"
    replace: Hi there!
  - trigger: ":date"
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "%Y-%m-%d"
  - trigger: ":hdate"
    replace: "{{mydate}}"
    vars:
      - name: myhdate
        type: date
        params:
          format: "%d-%m-%Y"
  - trigger: ":shell"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: echo Hello from your shell
  - trigger: ":mdl"
    replace: "[]()"
  - trigger: ":mdi"
    replace: " ![]()"
  - trigger: ":mdt"
    replace: " |  \t|  \t|  \t|\n|-\t|-\t|-\t|\n|  \t|  \t|  \t|\n|  \t|  \t|  \t|"
  - trigger: ":ml"
    replace: machine learning
  - trigger: ":help_zettel"
    replace: |
      :templ_comp  = template for comparison
      :templ_over  = template for overview
      :templ_zett  = generic template for zettel
      :templ_links = what every zettel needs to link to
      :templ_lit   = literary note (all interesting ideas from source)
      "templ_outl" = template outline
  - trigger: ":templ_comp"
    replace: |
      # thing one vs thing two
      #comparison
      description
      links to both
      ## Differences
      ## Similarities
      # References
      * link to more general idea
      * link to more specific idea
      * link to a related topic
  - trigger: ":templ_over"
    replace: |
      # Title overview
      #entrypoint #overview
      generic description
      ## Related
      * link to more general idea
      * link to more specific idea
      * link to a related topic
      
      ## overview
      * thing
      * other thing
  - trigger: ":templ_zett"
    replace: |
      # title (make it a claim: this is that )
      
      generic description

      ## Examples

      ## Relates to
      * link to more general idea
      * link to more specific idea
      * link to a related topic
      
      ## References
  - trigger: ":templ_links"
    replace: |
      ## Related
      * link to more general idea
      * link to more specific idea
      * link to a related topic
  - trigger: ":templ_lit"
    replace: |
      # Literature note
      _(save as `lit_*` use this to capture all interesting ideas from a reference )_
      (description of reference and actual reference)
      
      #  Interesting ideas
      * [page] idea
  - trigger: :templ_outl
    replace: |
      ## outline {}
      (save as `o_*`, use this as an outline for new work)
      # goal
      R who are my readers
      o Objective: What are you trying to do?
      A Action: What do you want the readers to do?
      M iMpression: What will they think of you?
      [roam](https://withoutbullshit.com/blog/roam-4-questions-to-ask-before-you-write-anything)
      # all zettls 
      (here)
      # actual outline
      (fat outline, structure and first few sentences)
